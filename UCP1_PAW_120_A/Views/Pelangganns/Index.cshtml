@model IEnumerable<UCP1_PAW_120_A.Models.Pelanggann>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NamaPembeli)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AlamatPembeli)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NamaPembeli)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AlamatPembeli)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPembeli">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPembeli">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPembeli">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
